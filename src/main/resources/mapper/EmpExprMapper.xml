<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mapper.EmpExprMapper">

    <!-- 批量插入工作经历：拼接成一条 insert 多组 values，避免循环调用单条 insert -->
    <insert id="insertBatch">
        insert into emp_expr(emp_id, company, position, start_date, end_date, description, create_time, update_time)
        values
        <foreach collection="exprList" item="expr" separator=",">
            (#{expr.empId}, #{expr.company}, #{expr.position}, #{expr.startDate},
             #{expr.endDate}, #{expr.description}, #{expr.createTime}, #{expr.updateTime})
        </foreach>
    </insert>

</mapper>
